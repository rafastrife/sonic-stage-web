# Quickstart: refatorar-agenda

To run and verify the new agenda page:
1. Start the API: `cd sonic-stage-api && python manage.py runserver`
2. Start the web app: `cd sonic-stage-web && npm start`
3. Navigate to `/agenda` in the browser.
