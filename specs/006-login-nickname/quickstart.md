# Phase 1: Quickstart

There are no new setup commands required. The feature will be available once the backend authentication backend is registered and the frontend is updated.
Existing users can immediately start logging in with their display names or emails.
