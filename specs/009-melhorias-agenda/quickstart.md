# Quickstart: melhorias-agenda

1. Start the API and Web App.
2. Go to `/agenda` and use the `<` and `>` buttons to change months.
3. Double click any day to quickly add an event.
4. Go to `/dashboard` to verify your new event is the "next_event".
